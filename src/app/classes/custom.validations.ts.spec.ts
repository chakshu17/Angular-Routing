import { Custom.Validations.Ts } from './custom.validations.ts';

describe('Custom.Validations.Ts', () => {
  it('should create an instance', () => {
    expect(new Custom.Validations.Ts()).toBeTruthy();
  });
});
